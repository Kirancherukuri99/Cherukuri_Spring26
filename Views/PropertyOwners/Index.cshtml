@model IEnumerable<Cherukuri_Spring26.Models.PropertyOwner>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Property)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Owner)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Property.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Owner.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PropertyOwnerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PropertyOwnerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PropertyOwnerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
